#ifndef HW2_HPP
#define HW2_HPP

#include <fstream>
#include <vector>
#include <algorithm>

int mid, size, number, first, last, numbertoadd;

std::ifstream inputFile;
std::ofstream outputFile;

std::vector<int>vect;

void StoogeSort(int*, int, int);

#endif 